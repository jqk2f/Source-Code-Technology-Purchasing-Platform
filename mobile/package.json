{
  "name": "@source-shop/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5174",
    "dev:h5": "vite --host 0.0.0.0 --port 5174",
    "build": "vue-tsc --noEmit && vite build",
    "build:h5": "vue-tsc --noEmit && vite build",
    "build:production": "vue-tsc --noEmit && vite build --mode production",
    "preview": "vite preview --host 0.0.0.0 --port 4174",
    "preview:h5": "vite preview --host 0.0.0.0 --port 4174",
    "lint": "vue-tsc --noEmit",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@vant/popperjs": "^1.3.0",
    "@vant/use": "^1.6.0",
    "pinia": "2.1.7",
    "vant": "^4.9.8",
    "vue": "3.4.21",
    "vue-router": "4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "5.0.4",
    "typescript": "5.4.5",
    "vite": "5.2.8",
    "vue-tsc": "2.0.11"
  }
}
